<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!--
        Permisos para Bluetooth en Android 12 y superior.
        BLUETOOTH_SCAN se necesita para buscar dispositivos.
        BLUETOOTH_CONNECT se necesita para conectarse a ellos.
        El atributo "usesPermissionFlags" con "neverForLocation" es una
        declaración importante de privacidad: le decimos al sistema que,
        aunque usamos el escaneo de BT, no lo usamos para obtener la
        ubicación del usuario.
    -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.BLUETOOTH_SCAN"
        tools:targetApi="s"
        android:usesPermissionFlags="neverForLocation" />
    <uses-permission android:name="android.permission.BLUETOOTH_CONNECT"
        tools:targetApi="s" />

    <!--
        Permisos para versiones antiguas de Android (inferiores a la 12).
        BLUETOOTH es para la comunicación y BLUETOOTH_ADMIN para descubrir
        dispositivos. ACCESS_FINE_LOCATION también era necesario para el
        escaneo de dispositivos BLE.
    -->
    <uses-permission android:name="android.permission.BLUETOOTH"
        android:maxSdkVersion="30" />
    <uses-permission android:name="android.permission.BLUETOOTH_ADMIN"
        android:maxSdkVersion="30" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION"
        android:maxSdkVersion="30" />


    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.TOTT">
        <receiver android:name=".broadcast.ReminderReceiver" android:enabled="true" />

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:theme="@style/Theme.TOTT">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>
